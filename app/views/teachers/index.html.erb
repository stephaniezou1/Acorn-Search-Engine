<div class = "search">

<h1 class="display-5"> Find a Teacher </h1>
<br/>

<div class="inline"><%= form_tag(teachers_path, method: :get) do %>
  Search By Username, Email, or Zipcode <%= text_field_tag :search, params[:search] %>
  <%= submit_tag 'Search 🔍', name: nil %>
</div>
<% end %>

<br/><br/>

<% if @teachers == [] %>
    <p> Waiting for you to enter a search term 📚🐣 </p>
<% end %>

<ul class="teacher-list">
    <% @teachers.each do |teacher| %>
        <li class="activity-list__item">
            <div class="activity-list__info">
                <%= link_to teacher.username, teacher, class: "teacher-list__name"%>
                <div class="activity-list__organization">
                    Email: <%= teacher.email %> <br/> Zipcode: <%= teacher.zipcode.zip_num %>
                </div>
                <div class="teacher-list__endorsements">
                    Number of Endorsements: <%= teacher.endorsements.length %>
                </div>
            </div>
        </li>
    <% end %>
</ul>

<br>

<%= button_to "Go To Activities Search Page", activities_path, method: :get, class: "button" %>

