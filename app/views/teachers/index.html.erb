<h1> Search For A Teacher </h1>

<br>

<%= form_tag(teachers_path, method: :get) do %>
  Search By Username, Email, or Zipcode <%= text_field_tag :search, params[:search] %>
  <%= submit_tag 'Search 🔍', name: nil %>
<% end %>

<br>

<% if @teachers == [] %>
    <p> Waiting for you to enter a search term 📚🐣 </p>
<% end %>

<ul class="teacher-list">
    <% @teachers.each do |teacher| %>
        <li class="teacher-list__item">
            <div class="teacher-list__info">
                <%= link_to teacher.username, teacher, class: "teacher-list__username"%>
                <div class="teacher-list__information">
                    <p> Email: <%= teacher.email %> <br/> Zipcode: <%= teacher.zipcode.zip_num %></p>
                </div>
                <div class="teacher-list__endorsements">
                    <p> Number of Endorsements: <%= teacher.endorsements.length %></p> 
                </div>
            </div>
        </li>
    <% end %>
</ul>

<br>

<%= button_to "Go To Activities Search Page", activities_path, method: :get, class: "button" %>

